[tool:pytest]
testpaths = tests
asyncio_mode = auto
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    auth: 认证相关测试
    middleware: 中间件/鉴权相关测试
    address: 业务接口相关测试
    integration: 集成测试（依赖外部服务）
    unit: 纯单元测试（不依赖外部服务）
